# Benchmarks

Use `benchmarks/tasks` to define fixed tasks and compare:

- Baseline prompt output
- Skill-guided output
- Time to completion
- Quality score

Store results in `benchmarks/results` for reproducibility.
